% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subtyper.R
\name{bipartite_variable_match}
\alias{bipartite_variable_match}
\title{Bipartite Variable Match}
\usage{
bipartite_variable_match(
  data,
  cog_col = "cog",
  voi_col = "voi",
  plot = FALSE,
  weight_type = "-log(p)",
  edge_width_multiplier = 11,
  directed = FALSE
)
}
\arguments{
\item{data}{A data frame containing the required columns for matching.}

\item{cog_col}{Character. The name of the first variable column. Default is "cog".}

\item{voi_col}{Character. The name of the second variable column. Default is "voi".}

\item{plot}{Logical. If TRUE, plots the resulting bipartite graph. Default is FALSE.}

\item{weight_type}{Character. Defines the edge weight type for the bipartite graph. Options are "d" (distance), "-log(p)" (negative log p-value). Default is "-log(p)".}

\item{edge_width_multiplier}{Numeric. Multiplier for the edge width in the plot. Default is 11.}

\item{directed}{Logical. If TRUE, creates a directed graph. Default is FALSE.}
}
\value{
A data frame with matched pairs of variables and their associated data.
}
\description{
This function matches two sets of variables using a bipartite graph and returns a data frame of the matched pairs.
}
\examples{
# Example usage:
df <- data.frame(cog = c("cog1", "cog2"), voi = c(1, 2), 
 d = c(0.1, 0.2), p = c(0.05, 0.01), multi = c(1, 2))
bipartite_variable_match(df, plot = TRUE)
}
