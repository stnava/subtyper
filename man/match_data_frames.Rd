% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subtyper.R
\name{match_data_frames}
\alias{match_data_frames}
\title{Match Two Data Frames Based on Nearest Neighbor Matching}
\usage{
match_data_frames(df1, df2, match_vars)
}
\arguments{
\item{df1}{A data frame.}

\item{df2}{A data frame.}

\item{match_vars}{A character vector of variable names to match on.}
}
\value{
A list containing the matched data frames and the result of the t-test.
}
\description{
This function matches two data frames on specified variables using the nearest neighbor
matching method and checks if the key variable is statistically significantly different
between the matched data frames.
}
\examples{
set.seed(123)
df1 <- data.frame(
  id = 1:100,
  age = rnorm(100, mean = 30, sd = 5),
  gender = sample(c("Male", "Female"), 100, replace = TRUE),
  score = rnorm(100, mean = 75, sd = 10)
)

df2 <- data.frame(
  id = 101:200,
  age = rnorm(100, mean = 30, sd = 5),
  gender = sample(c("Male", "Female"), 100, replace = TRUE),
  score = rnorm(100, mean = 70, sd = 15)
)

result <- match_data_frames(df1, df2, match_vars = c("age", "gender"), key_var = "score")
print(result$t_test)
}
