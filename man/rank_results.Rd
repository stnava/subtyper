% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subtyper.R
\name{rank_results}
\alias{rank_results}
\title{Rank Rows Based on Weighted Scores of Specified Columns}
\usage{
rank_results(df, columns_to_maximize, weights = NULL)
}
\arguments{
\item{df}{A data frame containing the columns to be ranked.}

\item{columns_to_maximize}{A character vector of column names that should be maximized and used for ranking.}

\item{weights}{A numeric vector of weights for the columns. The length of this vector must match the number of columns in \code{columns_to_maximize}. Defaults to \code{NULL}, in which case equal weights are applied.}
}
\value{
A data frame with the original columns, the normalized columns, the weighted score for each row, and the rank based on the weighted score.
}
\description{
This function ranks rows in a data frame based on the weighted scores of selected columns.
The function normalizes the specified columns (using Min-Max normalization), computes a weighted score for each row, and assigns a rank based on these scores.
If no weights are provided, the function defaults to equal weighting for the columns.
}
\examples{
# Create a sample data frame
df <- data.frame(
  A = c(1, 2, 3, 4),
  B = c(2, 3, 4, 5),
  C = c(3, 4, 5, 6)
)

# Specify columns to maximize
columns_to_maximize <- c("A", "B")

# Rank the rows based on the specified columns
ranked_df <- rank_results(df, columns_to_maximize, weights = c(0.7, 0.3))

# View the ranked data frame
print(ranked_df)

}
